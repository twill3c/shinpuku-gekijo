# SPEC.md — shinpuku-gekijo

<!-- scaffold template v1.12.0 から展開(2026-08-24)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

振幅劇場(shinpuku-gekijo)は、グローバー探索(量子探索アルゴリズム)の振幅増幅を
1 半歩(オラクル/拡散)ずつ観察できる可視化工房である。N 件から目印付きの 1 件を
約 √N 回で見つける量子の加速を、振幅棒グラフの反転と増幅で体感させる。
フリート「量子・計算科学」の第二号(第一号は mugen-tape)。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | グローバー探索エンジン(純粋関数、UI 非依存)。量子ビット数 n(2〜10、N=2^n)、目印集合 M(1〜N-1 件)。状態は実振幅ベクトル | must |
| F-02 | 半歩実行: オラクル(目印の符号反転)と拡散(平均まわりの反転)を別々に踏める。1 反復=オラクル+拡散 | must |
| F-03 | 振幅棒グラフ: 符号付き振幅を棒で表示。平均線・目印の強調・書き換わった瞬間の変化が追える | must |
| F-04 | 成功確率曲線: sin²((2t+1)θ) の理論曲線と現在の反復位置・最適反復 t* を表示。**最適を過ぎると確率が下がる(回りすぎ)** を見せられる | must |
| F-05 | 実行制御: 半歩 / 1 反復 / 最適回数まで自動 / 続けて回りすぎを見る / リセット。反復数・オラクル呼出数・成功確率のカウンタ | must |
| F-06 | 目印の選択: 棒のクリックで目印をトグル(複数可)。n 変更・目印変更でリセット | must |
| F-07 | 古典との対照: 同じ N に対する古典の期待照会数 N/2(平均)・N(最悪)と量子の t* を並記 | must |
| F-08 | 導入文(「グローバー探索とは」「ここでは何を可視化しているか」)と誠実さの注記(これは古典計算機による厳密シミュレーションであり、量子加速そのものは体験できない) | must |
| F-09 | 深リンク `#n=<int>&m=<csv>&t=<int>`(構成と反復位置の共有) | should |
| F-10 | フリート標準固定フッタ(MIT License・GitHub・歩き方・設計図・App Menu、画面下部固定) | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 完全静的サイト(バックエンド・ビルド工程なし、ES modules 直配信)。Vercel で配信 | 成果物構成の目視 + 本番疎通 |
| N-02 | 決定論: 乱数不使用。同一操作列 → 同一状態(測定は確率分布の表示であり、標本抽出はしない) | T-DET |
| N-03 | n=10(N=1024)でも半歩実行・描画が体感即時(<50ms) | 実測 |
| N-04 | UI はポインタ操作のみで全機能に到達できる | 手動確認 |

## 4. 品質基準

数値オラクル(すべて閉形式・数学的事実にトレースする):

- **G-01 閉形式オラクル**: t 反復後の振幅が厳密解と一致(許容 1e-12)。
  θ = arcsin(√(k/N))(k=|M|)として、目印振幅 = sin((2t+1)θ)/√k、
  非目印振幅 = cos((2t+1)θ)/√(N-k)。成功確率 = sin²((2t+1)θ)
- **G-02 最適反復の一致**: エンジンの t* (閉形式 round(π/(4θ) − 1/2)) が、
  t ∈ [0, ⌈π/4·√(N/k)⌉+2] の総当たりで求めた argmax sin²((2t+1)θ) と一致(全 (n,k) 格子)
- **G-03 不変量ゲート**: 全半歩でノルム保存 ‖a‖=1(1e-12)。目印内・非目印内の振幅は常に一様。
  オラクルは目印の符号だけを変え、拡散は平均まわりの反転そのもの(独立再計算と一致)
- **G-04 二実装照合**: JS エンジンと Python 参照実装の状態ベクトルが、
  (n,k) 代表格子+ランダム目印集合 ≥50 構成 × 各 t*+3 反復の全半歩で一致(1e-12)
- **G-05 回りすぎの実証**: t*+⌈t*/2⌉+1 反復後の成功確率 < t* 反復後の成功確率(理論・実装の両方で、
  ただし t* 時点で確率 1 に到達する縮退例 N=4,k=1 等は等号許容ではなく実測で別掲)

## 5. スコープ外

- 複素振幅・一般のユニタリゲート・回路エディタ(グローバーは実部分空間に閉じる)
- 測定の標本抽出(乱数を持ち込まない — N-02。分布の表示のみ)
- n > 10(棒が視認できない)・k = 0 または k = N(θ が定義されない構成は UI で禁止)
- 量子誤り・ノイズモデル、Qiskit 等外部フレームワーク連携
- 多言語対応・PWA 化
