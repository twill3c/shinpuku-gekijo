# TEST_SPEC.md — shinpuku-gekijo

<!-- scaffold template v1.12.0 から展開(2026-08-24) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- JS エンジンは pytest から `node harness/run_grover.mjs` 経由で駆動し、JSON を突き合わせる
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提をテスト内の assert で検算し、
  導出過程をコメントに残す(VERIF-FALSE / HC-004)
- 外部権威値・フリート慣例をテスト期待値に写すときは、書く前に (a) 権威値は一次資料
  または実測で確認し (b) 慣例は本プロジェクトでの前提(リポジトリ・URL・資産の実在)を
  確認する。確認できない前提は期待値にしない(mugen-tape HC-001 の規律を予防適用)
- 「経路 A と経路 B の一致」を検査するテストは、両経路が実在して実行された証拠
  (mode エコー等)を検査に含める(mugen-tape VERIF-GAP の教訓を予防適用)

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| t 反復後の振幅・成功確率 | 閉形式 sin((2t+1)θ)/√k, cos((2t+1)θ)/√(N-k), θ=arcsin√(k/N)。グローバー探索の標準的厳密解(導出はテスト docstring に記載) | 厳密 |
| 最適反復 t* | 閉形式 round(π/(4θ)−1/2) を**総当たり argmax と照合**(G-02。閉形式自体をオラクル任せにしない) | 厳密+総当たり |
| 拡散演算 | 独立再計算(2·mean − a)との一致。オラクルは符号反転の定義そのもの | 厳密 |
| 二実装照合 | harness/reference_grover.py(Python 参照実装・独立記述) | 相互検証 |
| N=4, k=1 の特例 | θ=π/6 → t*=1 で成功確率が厳密に 1(sin²(π/2))。教科書的縮退例 | 厳密 |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 | 初期状態: 全振幅 1/√N・ノルム 1 | 厳密一致(1e-12) |
| T-002 | F-02 | オラクル半歩: 目印の符号のみ反転、他は不変 | 定義どおり |
| T-003 | F-02 | 拡散半歩: 独立再計算 2·mean−a と一致 | 1e-12 |
| T-004 | G-01 | t 反復後の振幅・成功確率が閉形式と一致((n,k) 格子 × t=0..t*+3) | 1e-12 |
| T-005 | G-02 | t* が総当たり argmax と一致(n=2..10 × k 代表値) | 完全一致 |
| T-006 | G-03 | 全半歩でノルム 1・クラス内一様(ランダム目印 50 構成) | 1e-12 |
| T-007 | G-04 | JS と Python の状態ベクトル完全一致(代表+ランダム 50 構成の全半歩) | 1e-12 |
| T-008 | G-05 | 回りすぎ: 過剰反復で成功確率が下がる(縮退例は除外し、除外理由を assert) | 理論・実装両方 |
| T-009 | F-07 | 古典対照: N/2・N・t* の並記値が定義どおり | 厳密 |
| T-010 | N-02 | 決定論: 同一操作列 2 回で状態ベクトル一致 | 完全一致 |
| T-011 | F-01 | 縮退例 N=4,k=1: t=1 で成功確率 = 1(1e-12) | 厳密 |
| T-012 | F-09 | 深リンク: #n&m&t の往復恒等(符号化→復号) | 恒等 |
| T-013 | F-08/F-10 | UI 静的検査: 導入文・誠実さ注記・固定フッタ(5 リンク+position:fixed+余白トークン) | 掲出 |
| T-014 | F-01 | 不正構成の拒否: k=0, k=N, n<2, n>10 はエンジンが例外 | 例外 |
